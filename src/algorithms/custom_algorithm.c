/* =====================================================================
 * algorithms/custom_algorithm.c
 * =====================================================================
 *
 * Frente responsável: Diogo Gomes (algoritmo próprio).
 *
 * Objetivo:
 *   Implementar o algoritmo de escalonamento próprio da equipe, com
 *   contribuição própria em relação aos algoritmos clássicos
 *   obrigatórios (FCFS, Round Robin e Prioridade).
 *
 *   Renomear este arquivo (e a função correspondente) para o nome
 *   escolhido para o algoritmo assim que a equipe decidir.
 *
 * O que deve ser implementado aqui:
 *   - Função que segue o contrato definido em scheduler.h
 *   - A decisão de qual processo executar deve usar apenas
 *     informações disponíveis no estado atual da simulação e no
 *     histórico já observado (NÃO pode usar conhecimento futuro que
 *     um sistema operacional real não teria)
 *
 * Antes de implementar, definir e documentar em docs/MODELAGEM.md:
 *   - Qual problema o algoritmo tenta resolver
 *   - Quais informações o algoritmo usa para tomar decisões
 *   - Como o próximo processo é escolhido
 *   - Por que o algoritmo deveria melhorar algum aspecto da simulação
 *   - Quais limitações ele possui
 *   - Se baseado em uma ideia/fonte existente: qual, o que foi
 *     modificado e por quê (a fonte deve ser citada no artigo)
 *
 * Requisitos do enunciado relacionados:
 *   - Seção 8 (Algoritmo próprio) — ver docs/PROJETO.md
 *
 * TODO: implementar. Este arquivo é apenas um esqueleto inicial.
 * ===================================================================== */

/* #include "../scheduler.h" */
